/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by LCM
 **/

#include <string.h>
#include "mrlcm_link_t.h"

static int __mrlcm_link_t_hash_computed;
static int64_t __mrlcm_link_t_hash;
 
int64_t __mrlcm_link_t_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __mrlcm_link_t_get_hash)
            return 0;
 
    const __lcm_hash_ptr cp = { p, (void*)__mrlcm_link_t_get_hash };
    (void) cp;
 
    int64_t hash = 0x43ac3d9ca40a9ad6LL
         + __int64_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int64_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __int64_t_hash_recursive(&cp)
        ;
 
    return (hash<<1) + ((hash>>63)&1);
}
 
int64_t __mrlcm_link_t_get_hash(void)
{
    if (!__mrlcm_link_t_hash_computed) {
        __mrlcm_link_t_hash = __mrlcm_link_t_hash_recursive(NULL);
        __mrlcm_link_t_hash_computed = 1;
    }
 
    return __mrlcm_link_t_hash;
}
 
int __mrlcm_link_t_encode_array(void *buf, int offset, int maxlen, const mrlcm_link_t *p, int elements)
{
    int pos = 0, thislen, element;
 
    for (element = 0; element < elements; element++) {
 
        thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].id), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].collection1), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
        thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].id1), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].collection2), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
        thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].id2), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
    }
    return pos;
}
 
int mrlcm_link_t_encode(void *buf, int offset, int maxlen, const mrlcm_link_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __mrlcm_link_t_get_hash();
 
    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
 
    thislen = __mrlcm_link_t_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;
 
    return pos;
}
 
int __mrlcm_link_t_encoded_array_size(const mrlcm_link_t *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {
 
        size += __int64_t_encoded_array_size(&(p[element].id), 1);
 
        size += __int32_t_encoded_array_size(&(p[element].collection1), 1);
 
        size += __int64_t_encoded_array_size(&(p[element].id1), 1);
 
        size += __int32_t_encoded_array_size(&(p[element].collection2), 1);
 
        size += __int64_t_encoded_array_size(&(p[element].id2), 1);
 
    }
    return size;
}
 
int mrlcm_link_t_encoded_size(const mrlcm_link_t *p)
{
    return 8 + __mrlcm_link_t_encoded_array_size(p, 1);
}
 
int __mrlcm_link_t_decode_array(const void *buf, int offset, int maxlen, mrlcm_link_t *p, int elements)
{
    int pos = 0, thislen, element;
 
    for (element = 0; element < elements; element++) {
 
        thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].id), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].collection1), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
        thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].id1), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].collection2), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
        thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].id2), 1);
        if (thislen < 0) return thislen; else pos += thislen;
 
    }
    return pos;
}
 
int __mrlcm_link_t_decode_array_cleanup(mrlcm_link_t *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {
 
        __int64_t_decode_array_cleanup(&(p[element].id), 1);
 
        __int32_t_decode_array_cleanup(&(p[element].collection1), 1);
 
        __int64_t_decode_array_cleanup(&(p[element].id1), 1);
 
        __int32_t_decode_array_cleanup(&(p[element].collection2), 1);
 
        __int64_t_decode_array_cleanup(&(p[element].id2), 1);
 
    }
    return 0;
}
 
int mrlcm_link_t_decode(const void *buf, int offset, int maxlen, mrlcm_link_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __mrlcm_link_t_get_hash();
 
    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;
 
    thislen = __mrlcm_link_t_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;
 
    return pos;
}
 
int mrlcm_link_t_decode_cleanup(mrlcm_link_t *p)
{
    return __mrlcm_link_t_decode_array_cleanup(p, 1);
}
 
int __mrlcm_link_t_clone_array(const mrlcm_link_t *p, mrlcm_link_t *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {
 
        __int64_t_clone_array(&(p[element].id), &(q[element].id), 1);
 
        __int32_t_clone_array(&(p[element].collection1), &(q[element].collection1), 1);
 
        __int64_t_clone_array(&(p[element].id1), &(q[element].id1), 1);
 
        __int32_t_clone_array(&(p[element].collection2), &(q[element].collection2), 1);
 
        __int64_t_clone_array(&(p[element].id2), &(q[element].id2), 1);
 
    }
    return 0;
}
 
mrlcm_link_t *mrlcm_link_t_copy(const mrlcm_link_t *p)
{
    mrlcm_link_t *q = (mrlcm_link_t*) malloc(sizeof(mrlcm_link_t));
    __mrlcm_link_t_clone_array(p, q, 1);
    return q;
}
 
void mrlcm_link_t_destroy(mrlcm_link_t *p)
{
    __mrlcm_link_t_decode_array_cleanup(p, 1);
    free(p);
}
 
int mrlcm_link_t_publish(lcm_t *lc, const char *channel, const mrlcm_link_t *p)
{
      int max_data_size = mrlcm_link_t_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = mrlcm_link_t_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _mrlcm_link_t_subscription_t {
    mrlcm_link_t_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void mrlcm_link_t_handler_stub (const lcm_recv_buf_t *rbuf, 
                            const char *channel, void *userdata)
{
    int status;
    mrlcm_link_t p;
    memset(&p, 0, sizeof(mrlcm_link_t));
    status = mrlcm_link_t_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding mrlcm_link_t!!!\n", status);
        return;
    }

    mrlcm_link_t_subscription_t *h = (mrlcm_link_t_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    mrlcm_link_t_decode_cleanup (&p);
}

mrlcm_link_t_subscription_t* mrlcm_link_t_subscribe (lcm_t *lcm, 
                    const char *channel, 
                    mrlcm_link_t_handler_t f, void *userdata)
{
    mrlcm_link_t_subscription_t *n = (mrlcm_link_t_subscription_t*)
                       malloc(sizeof(mrlcm_link_t_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel, 
                                 mrlcm_link_t_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg mrlcm_link_t LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int mrlcm_link_t_unsubscribe(lcm_t *lcm, mrlcm_link_t_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr, 
           "couldn't unsubscribe mrlcm_link_t_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}

